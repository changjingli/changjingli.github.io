---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getAllTags, getPosts } from '@/utils/posts';

const posts = await getPosts();
const tags = getAllTags(posts).slice(0, 8);
---
<BaseLayout title="关于" current="about">
  <section class="site-shell page-title">
    <p class="kicker">About</p>
    <h1>关于这个博客</h1>
    <p>这里主要放一些我想明白了、还没想明白但值得记下来的东西。</p>
  </section>
  <section class="site-shell" style="padding-bottom: 80px;">
    <div class="panel prose heti">
      <p>我叫常敬礼，前前端开发工程师，现全栈开发工程师。说人话就是：以前主要和页面、交互、浏览器较劲，现在还要和接口、数据、部署、AI Agent 一起较劲。</p>
      <p>很多事情一开始只是工作里的问题，后来慢慢变成了我理解技术、协作和生活的入口。</p>
      <p>这个博客不会只写某一类内容。有时是一次项目复盘，有时是一篇读书笔记，有时只是某个月忽然冒出来的想法。写下来不是为了显得很有观点，而是怕自己过一阵就忘了当时怎么想、怎么卡住、又是怎么走出来的。</p>
      <p>如果你也关心 Web、AI、效率、成长，或者只是想看看一个普通工程师怎么和工作、家庭、学习相处，可以从这些主题随便逛逛：</p>
      <div class="tag-row">
        {tags.map((tag) => <a class="tag" href={`/tags/${encodeURIComponent(tag.name)}/`}>{tag.name}</a>)}
      </div>
    </div>
  </section>
</BaseLayout>
