# 敬礼的唠叨

个人博客，使用 Astro 构建。

## 本地开发

```bash
npm install
npm run dev
```

## 常用命令

- `npm run dev`：启动本地开发服务
- `npm run build`：生成静态站点到 `dist/`
- `npm run preview`：预览构建产物
- `npm run check`：运行 Astro 类型检查

## 部署

项目通过 Vercel 部署，构建命令为 `npm run build`，输出目录为 `dist/`。
